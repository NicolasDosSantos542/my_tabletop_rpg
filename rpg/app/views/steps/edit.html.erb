<h1>Edit step</h1>

<%= render "form", step: @step %>


<div><%= link_to "Destroy step", step_path(@step), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></div>