<%= form_with(model: step) do |form| %>
  <% if step.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(step.errors.count, "error") %> prohibited this step from being saved:</h2>

      <ul>
        <% step.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :choice_id, style: "display: block" %>
    <%= form.text_field :choice_id %>
  </div>

  <div>
    <%= form.label :creature_id, style: "display: block" %>
    <%= form.text_field :creature_id %>
  </div>

  <div>
    <%= form.label :next, style: "display: block" %>
    <%= form.number_field :next %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
