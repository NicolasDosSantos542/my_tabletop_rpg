<p style="color: green"><%= notice %></p>

<h1>Loots</h1>

<%= link_to "New loot", new_loot_path %>

<div id="loots">
  <% @loots.each do |loot| %>
    <%= render loot %>
    <p>
      <%= link_to "Show this loot", loot %>
    </p>
  <% end %>
</div>

